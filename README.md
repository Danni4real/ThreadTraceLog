# ThreadTraceLog

A C++ logger, features:

1. Can set thread name;
2. Use spdlog as default log printer;
3. Different color for different thread;
4. Print function name and it's arguments;
5. Print exit log when exit scope/function;
6. Indent when goes into new scope/function;
